# Tournament OS — v12.6 mobile menu freeze

## What changed
- rebuilt the **top mobile menu** into a clearer two-level premium app bar
- added a contextual block for current screen meaning
- added compact KPI lane under the header for live / pending / open state
- rebuilt the **bottom mobile menu** into a cleaner floating product dock
- strengthened active state, icon container hierarchy, spacing, and contrast
- added compact navigation counters without changing any route logic

## What did not change
- no product flow changes
- no route structure changes
- no detail tab changes
- no player/admin scenario changes
- no storage model changes

## Fixed baseline for next iterations
- mobile top menu is now part of the design contract
- mobile bottom menu is now part of the design contract
- if approved logic, agreements, or docs change, repo docs must be updated in the same package

## Deploy
Upload all files from this archive to the GitHub Pages root, replacing the previous version.
